<Page x:Class="AIManager.UI.Views.Pages.WebLearningPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="1400"
      Title="Web Learning - Workflow Teaching"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Step Status Colors -->
        <SolidColorBrush x:Key="StepClick" Color="#4CAF50"/>
        <SolidColorBrush x:Key="StepType" Color="#2196F3"/>
        <SolidColorBrush x:Key="StepUpload" Color="#FF9800"/>
        <SolidColorBrush x:Key="StepSelect" Color="#9C27B0"/>
        <SolidColorBrush x:Key="StepNavigate" Color="#00BCD4"/>
    </Page.Resources>

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="20,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#20FF6B6B" Offset="0"/>
                    <GradientStop Color="#204ECDC4" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock Text="Web Learning" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="สอนระบบทำงานบนเว็บไซต์โดยบันทึกการกระทำของคุณ"
                               Foreground="#AAFFFFFF" Margin="0,5,0,0"/>
                </StackPanel>

                <!-- Control Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <!-- Platform Selection -->
                    <ComboBox x:Name="PlatformComboBox" Width="150" Margin="0,0,15,0"
                              materialDesign:HintAssist.Hint="เลือก Platform"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Foreground="White" BorderBrush="#40FFFFFF">
                        <ComboBoxItem Content="Facebook" Tag="facebook"/>
                        <ComboBoxItem Content="Instagram" Tag="instagram"/>
                        <ComboBoxItem Content="TikTok" Tag="tiktok"/>
                        <ComboBoxItem Content="Twitter/X" Tag="twitter"/>
                        <ComboBoxItem Content="LINE" Tag="line"/>
                        <ComboBoxItem Content="YouTube" Tag="youtube"/>
                        <ComboBoxItem Content="Threads" Tag="threads"/>
                        <ComboBoxItem Content="LinkedIn" Tag="linkedin"/>
                        <ComboBoxItem Content="Pinterest" Tag="pinterest"/>
                        <ComboBoxItem Content="Custom" Tag="custom" IsSelected="True"/>
                    </ComboBox>

                    <!-- Task Type -->
                    <ComboBox x:Name="TaskTypeComboBox" Width="150" Margin="0,0,15,0"
                              materialDesign:HintAssist.Hint="ประเภทงาน"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Foreground="White" BorderBrush="#40FFFFFF">
                        <ComboBoxItem Content="Post Content" Tag="post" IsSelected="True"/>
                        <ComboBoxItem Content="Login" Tag="login"/>
                        <ComboBoxItem Content="Upload Media" Tag="upload"/>
                        <ComboBoxItem Content="Comment" Tag="comment"/>
                        <ComboBoxItem Content="Search" Tag="search"/>
                        <ComboBoxItem Content="Custom" Tag="custom"/>
                    </ComboBox>

                    <Button x:Name="StartRecordingButton" Click="StartRecordingButton_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#4CAF50" Foreground="White"
                            Margin="0,0,10,0" Padding="15,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Record" VerticalAlignment="Center"/>
                            <TextBlock Text="เริ่มบันทึก" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="StopRecordingButton" Click="StopRecordingButton_Click"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#F44336" Foreground="White"
                            IsEnabled="False" Padding="15,8">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Stop" VerticalAlignment="Center"/>
                            <TextBlock Text="หยุด" Margin="8,0,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>

            <!-- Browser Panel -->
            <Grid Grid.Column="0" Margin="0,0,15,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- URL Bar -->
                <Border Grid.Row="0" Background="#30000000" CornerRadius="10" Padding="10" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0" Style="{StaticResource MaterialDesignIconButton}"
                                Click="BackButton_Click" ToolTip="Back" Width="32" Height="32">
                            <materialDesign:PackIcon Kind="ArrowLeft" Width="18" Height="18"/>
                        </Button>
                        <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconButton}"
                                Click="ForwardButton_Click" ToolTip="Forward" Width="32" Height="32">
                            <materialDesign:PackIcon Kind="ArrowRight" Width="18" Height="18"/>
                        </Button>
                        <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}"
                                Click="RefreshButton_Click" ToolTip="Refresh" Width="32" Height="32">
                            <materialDesign:PackIcon Kind="Refresh" Width="18" Height="18"/>
                        </Button>

                        <TextBox x:Name="UrlTextBox" Grid.Column="3" Margin="10,0"
                                 Text="https://www.facebook.com"
                                 VerticalContentAlignment="Center"
                                 materialDesign:HintAssist.Hint="Enter URL"
                                 KeyDown="UrlTextBox_KeyDown"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Foreground="White" BorderBrush="#40FFFFFF"/>

                        <Button Grid.Column="4" Style="{StaticResource MaterialDesignRaisedButton}"
                                Click="GoButton_Click" Content="Go" Padding="15,5"
                                Background="#4ECDC4"/>
                    </Grid>
                </Border>

                <!-- WebView2 Browser -->
                <Border Grid.Row="1" Background="#20000000" CornerRadius="10" ClipToBounds="True">
                    <Grid>
                        <wv2:WebView2 x:Name="WebBrowser"
                                      NavigationCompleted="WebBrowser_NavigationCompleted"
                                      CoreWebView2InitializationCompleted="WebBrowser_CoreWebView2InitializationCompleted"/>

                        <!-- Recording Indicator Overlay -->
                        <Border x:Name="RecordingIndicator" Background="#80FF0000"
                                HorizontalAlignment="Right" VerticalAlignment="Top"
                                CornerRadius="0,10,0,10" Padding="15,8"
                                Visibility="Collapsed">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" Fill="White">
                                    <Ellipse.Triggers>
                                        <EventTrigger RoutedEvent="Loaded">
                                            <BeginStoryboard>
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                     From="1" To="0.3" Duration="0:0:0.5"
                                                                     AutoReverse="True"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </EventTrigger>
                                    </Ellipse.Triggers>
                                </Ellipse>
                                <TextBlock Text="กำลังบันทึก..." Foreground="White"
                                           FontWeight="Bold" Margin="8,0,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Loading Indicator -->
                        <Border x:Name="LoadingIndicator" Background="#80000000"
                                Visibility="Collapsed">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                                <TextBlock Text="กำลังโหลด..." Foreground="White"
                                           Margin="0,10,0,0" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <!-- Right Panel: Recorded Steps -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Steps Header -->
                <Border Grid.Row="0" Background="#30000000" CornerRadius="10" Padding="15" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel>
                            <TextBlock Text="Recorded Steps" FontWeight="Bold" FontSize="16" Foreground="White"/>
                            <TextBlock x:Name="StepCountText" Text="0 steps recorded"
                                       Foreground="#AAFFFFFF" FontSize="12" Margin="0,3,0,0"/>
                        </StackPanel>

                        <Button Grid.Column="1" Style="{StaticResource MaterialDesignIconButton}"
                                Click="ClearStepsButton_Click" ToolTip="Clear All Steps"
                                Width="32" Height="32">
                            <materialDesign:PackIcon Kind="Delete" Width="18" Height="18" Foreground="#F44336"/>
                        </Button>
                    </Grid>
                </Border>

                <!-- Steps List -->
                <Border Grid.Row="1" Background="#20000000" CornerRadius="10" Padding="10">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="StepsItemsControl">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#30000000" CornerRadius="8" Padding="10" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Step Number -->
                                            <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                                                    Background="{Binding ActionColor}" Margin="0,0,10,0">
                                                <TextBlock Text="{Binding StepNumber}"
                                                           HorizontalAlignment="Center" VerticalAlignment="Center"
                                                           Foreground="White" FontWeight="Bold" FontSize="11"/>
                                            </Border>

                                            <!-- Step Details -->
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding ActionText}"
                                                           FontWeight="SemiBold" Foreground="White"/>
                                                <TextBlock Text="{Binding ElementDescription}"
                                                           Foreground="#AAFFFFFF" FontSize="11"
                                                           TextTrimming="CharacterEllipsis" MaxWidth="220"/>
                                                <TextBlock Text="{Binding ValueText}"
                                                           Foreground="#80FFFFFF" FontSize="10"
                                                           Visibility="{Binding HasValue, Converter={StaticResource BoolToVis}}"
                                                           TextTrimming="CharacterEllipsis" MaxWidth="220"/>
                                            </StackPanel>

                                            <!-- Delete Button -->
                                            <Button Grid.Column="2" Style="{StaticResource MaterialDesignIconButton}"
                                                    Click="DeleteStepButton_Click" Tag="{Binding Index}"
                                                    Width="24" Height="24" ToolTip="ลบ step นี้">
                                                <materialDesign:PackIcon Kind="Close" Width="14" Height="14"
                                                                         Foreground="#80FFFFFF"/>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- Save Workflow Panel -->
                <Border Grid.Row="2" Background="#30000000" CornerRadius="10" Padding="15" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBox x:Name="WorkflowNameTextBox"
                                 materialDesign:HintAssist.Hint="ชื่อ Workflow"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Foreground="White" BorderBrush="#40FFFFFF"
                                 Margin="0,0,0,10"/>

                        <TextBox x:Name="WorkflowDescriptionTextBox"
                                 materialDesign:HintAssist.Hint="คำอธิบาย (optional)"
                                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 Foreground="White" BorderBrush="#40FFFFFF"
                                 TextWrapping="Wrap" AcceptsReturn="True"
                                 Height="60" Margin="0,0,0,10"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Button Grid.Column="0" Content="Test Workflow"
                                    Click="TestWorkflowButton_Click"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    Foreground="#4ECDC4" BorderBrush="#4ECDC4"
                                    Margin="0,0,5,0"/>

                            <Button Grid.Column="1" Content="Save Workflow"
                                    Click="SaveWorkflowButton_Click"
                                    Style="{StaticResource MaterialDesignRaisedButton}"
                                    Background="#4CAF50" Foreground="White"
                                    Margin="5,0,0,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
