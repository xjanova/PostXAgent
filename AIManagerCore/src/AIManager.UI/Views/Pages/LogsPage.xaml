<Page x:Class="AIManager.UI.Views.Pages.LogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Logs">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Application Logs" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Clear" Click="ClearLogs_Click" Margin="20,0,0,0"
                    Style="{StaticResource MaterialDesignOutlinedButton}"/>
        </StackPanel>

        <materialDesign:Card Grid.Row="1" Padding="0">
            <ListBox ItemsSource="{Binding Logs}" Background="#1E1E1E" Foreground="White"
                     FontFamily="Consolas" FontSize="12">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Timestamp}" Foreground="#808080" Width="180"/>
                            <TextBlock Text="{Binding Level}" Foreground="{Binding LevelColor}" Width="60" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Message}" Foreground="White"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </materialDesign:Card>
    </Grid>
</Page>
